# Local overrides for docker-compose (copy to .env — never commit .env).
# No real secrets are needed locally: DynamoDB Local accepts any credentials,
# and in the cloud the pod gets AWS access via IRSA (no static keys at all).

LOG_LEVEL=INFO

# Host port for the frontend (change it if 3000 is taken on your machine)
FRONTEND_PORT=3000
